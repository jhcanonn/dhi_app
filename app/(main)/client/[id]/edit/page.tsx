import { ClientEdit } from '@components/templates'
import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'DHI | Edicion del Paciente',
}

const ClientEditPage = () => <ClientEdit />

export default ClientEditPage
