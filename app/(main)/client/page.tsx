import { ComingSoon } from '@components/templates'
import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'DHI | Cliente',
}

const Client = () => <ComingSoon page='Cliente' />

export default Client
