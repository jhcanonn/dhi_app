import { ComingSoon } from '@components';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'DHI | Estadisticas',
};

const Statistics = () => <ComingSoon page="Estadisticas" />;

export default Statistics;
